// Phase 4 T4.3 — Sankey scale fallback constants.
// Components consume these to decide Sankey vs stacked-column fallback.

export const SANKEY_MAX_BUCKETS = 8;
export const SANKEY_FALLBACK_THRESHOLD = 50; // sessions/week
