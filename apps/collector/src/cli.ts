#!/usr/bin/env bun
// Bematist — CLI entrypoint (bun --compile target). Binary name: bematist.
// Real commands (install, status, audit, dry-run, policy, doctor, purge, erase,
// outcomes, waste, prompts, export, scan, serve) come in Sprint 1+.
// biome-ignore lint/suspicious/noConsole: stub CLI entry; real commands land Sprint 1+.
console.log("bematist-cli skeleton");
