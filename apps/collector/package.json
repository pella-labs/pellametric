{
  "name": "@pella/collector",
  "version": "0.0.1",
  "private": true,
  "bin": {
    "pella-collect": "./dist/index.js"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "build": "bun run build.ts",
    "dev": "bun --hot src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pella/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
