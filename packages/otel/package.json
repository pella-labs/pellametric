{
  "name": "@bematist/otel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "generate": "buf generate",
    "check:generated": "buf generate && git diff --exit-code src/gen"
  },
  "dependencies": {
    "@bematist/schema": "workspace:*",
    "@bufbuild/protobuf": "^2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1",
    "@bufbuild/protoc-gen-es": "^2",
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
