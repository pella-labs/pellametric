{
  "name": "@bematist/fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./redaction": "./redaction/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "seed:perf": "bun run seed/run.ts",
    "record:github": "bun run src/github/record.ts"
  },
  "dependencies": {
    "@bematist/schema": "workspace:*",
    "@clickhouse/client": "^1.18.2",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
