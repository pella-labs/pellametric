# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz
bun.lockb

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Bun compiled binaries
*.bun

# Env / secrets
.env
.env.local
.env.*.local
.env.development
.env.production

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun-debug.log*

# Coverage / test
coverage/
.nyc_output/
*.lcov
playwright-report/
test-results/
tests/perf/summary.json

# Local data (Bematist-specific)
.bematist/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.db-shm
*.db-wal

# Docker volumes
postgres-data/
clickhouse-data/
redis-data/

# Per-dev docker compose overrides
docker-compose.*.local.yml

# Claude Code local
.claude/settings.local.json
.claude/skills/
.agents/skills/
.gitnexus

# NOTE: packages/otel/src/gen/** is generated by `bun run generate:otel`
# (buf + protoc-gen-es against vendor/opentelemetry-proto) but is COMMITTED so
# CI doesn't need protoc. Do NOT ignore it here — CI `check:otel-generated`
# re-runs generate and fails if the committed tree drifted.

# Personal scope / review docs (untracked)
DAVID_SCOPE.md
STUDY_GUIDE.md

# Git worktrees (isolated feature branches)
.worktrees/

docs/superpowers/
